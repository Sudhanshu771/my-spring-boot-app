# my-spring-boot-app

~ I have created a new project for ecommerce website ~

1) Before launching the application , you need to build it first , if the build fails you have to run this command "mvn clean install -U" and try it once.